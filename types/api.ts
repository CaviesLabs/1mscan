export type IJsonChainConfigs = {
  common: {
    MAINTENANCE_MODE: boolean
    MAINTENANCE_CHAIN_MODE: boolean
  }
  workspace: {
    WORKSPACE_ID: string
    WORKSPACE_ENDPOINT: string
    WORKSPACE_RESOURCE: string
  }
}
